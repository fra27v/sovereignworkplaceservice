# OpenBao Operator Scripts

Operator-plane scripts for OpenBao administration workflows belong here.

Scripts must read sensitive values from runtime inputs or ignored local files, not from committed source.
