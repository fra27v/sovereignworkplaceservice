# Operator PKI Scripts

Shared Operator PKI scripts may be added here when behavior applies across
environments.

The first implementation keeps executable scripts in the
`vps-family-control` environment because the OpenBao pod name, namespace, local
bootstrap paths, and publication paths are environment-specific.
