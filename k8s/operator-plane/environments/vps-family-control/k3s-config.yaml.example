# Example k3s configuration for the operator-plane VPS environment.
# Copy locally and adjust outside version control before use.

cluster-init: true
write-kubeconfig-mode: "0600"
disable:
  - servicelb
  - traefik
