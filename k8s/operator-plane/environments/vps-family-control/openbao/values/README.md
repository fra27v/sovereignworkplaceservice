# OpenBao Environment Values

Helm values for the `vps-family-control` OpenBao deployment belong here.

Only commit non-secret `*.yaml` values. Put live credentials and generated secret material in ignored local files.
