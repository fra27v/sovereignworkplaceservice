# Global OpenBao values for the vps-family-control operator-plane.
# This file contains no secrets. Local VPS files are expected under:
# /var/lib/sovereignworkplaceservice/openbao/

global:
  enabled: true
  tlsDisable: false

injector:
  enabled: false

server:
  enabled: true
  image:
    repository: openbao/openbao

  ingress:
    enabled: false

  service:
    enabled: true
    type: ClusterIP

  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 768Mi

  dataStorage:
    enabled: true
    mountPath: /openbao/data
    size: 5Gi
    accessMode: ReadWriteOnce

  auditStorage:
    enabled: true
    mountPath: /openbao/audit
    size: 2Gi
    accessMode: ReadWriteOnce

  standalone:
    enabled: true
    config: |
      ui = true

      listener "tcp" {
        address       = "[::]:8200"
        tls_disable   = false
        tls_cert_file = "/openbao/tls/tls.crt"
        tls_key_file  = "/openbao/tls/tls.key"
      }

      storage "file" {
        path = "/openbao/data"
      }

      seal "static" {
        current_key_id = "20260618-1"
        current_key    = "file:///openbao/seal/unseal-20260618-1.key"
      }

      audit "file" "file" {
        description = "Global OpenBao file audit device"
        options = {
          file_path = "/openbao/audit/audit.log"
          log_raw   = "false"
        }
      }

      telemetry {
        disable_hostname = true
      }

  volumes:
    - name: openbao-static-seal
      hostPath:
        path: /var/lib/sovereignworkplaceservice/openbao/seal
        type: Directory
    - name: openbao-local-tls
      hostPath:
        path: /var/lib/sovereignworkplaceservice/openbao/tls
        type: Directory

  volumeMounts:
    - name: openbao-static-seal
      mountPath: /openbao/seal
      readOnly: true
    - name: openbao-local-tls
      mountPath: /openbao/tls
      readOnly: true

  ha:
    enabled: false
