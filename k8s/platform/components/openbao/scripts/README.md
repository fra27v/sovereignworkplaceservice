# OpenBao Platform Scripts

Reusable helper scripts for managing the OpenBao Helm component belong here.

Scripts in this directory must not embed tokens, unseal keys, root tokens, or other live secrets.
